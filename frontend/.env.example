# Poker ZK — frontend env (copy to .env and fill after deploy)
# Without VITE_POKER_ZK_CONTRACT_ID the app shows "Contract Not Configured".

# Contract ID (poker-game-manager) — set after running ./scripts/deploy.sh
VITE_POKER_ZK_CONTRACT_ID=
# Game hub contract (optional) — deploy.sh writes this when it deploys game-hub; needed so poker-game-manager keeps the same hub
# VITE_GAME_HUB_CONTRACT_ID=

# RPC (optional; defaults to testnet)
# VITE_SOROBAN_RPC_URL=https://soroban-testnet.stellar.org
# VITE_NETWORK_PASSPHRASE=Test SDF Network ; September 2015

# Dev wallets (optional) — for Quickstart, Import & Sign, commit/reveal without Freighter
# Auto-generate: cd frontend && bun run setup  (writes here + root .env; then fund P1/P2 via Friendbot and restart dev server)
VITE_DEV_PLAYER1_ADDRESS=
VITE_DEV_PLAYER2_ADDRESS=
VITE_DEV_PLAYER1_SECRET=
VITE_DEV_PLAYER2_SECRET=

# Deployer secret — needed once to run init-game-manager (initialize config so sit_at_table works).
# Get it with: stellar keys export deployer   (if you used identity "deployer" for deploy)
# DEPLOYER_SECRET=S...

# Optional: admin / simulation source
# VITE_DEV_ADMIN_ADDRESS=
# VITE_SIMULATION_SOURCE_ADDRESS=
